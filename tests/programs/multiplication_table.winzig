program multiplication_table:
# Print the 1..4 multiplication table (row-major), one product per line.
var i, j : integer;
begin
  for (i := 1; i <= 4; i := i + 1)
    for (j := 1; j <= 4; j := j + 1)
      output(i * j);
end multiplication_table.
