program fibonacci_rec:
var x : integer;
function fib(n:integer):integer;
begin
  if n < 2 then return (n)
  else return (fib(n - 1) + fib(n - 2));
end fib;
begin
  read(x);
  while x > 0 do begin
    output(fib(x));
    read(x);
  end;
end fibonacci_rec.
